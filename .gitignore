__pycache__/

# Intermediates regenerated by the analysis scripts. They carry full report
# free text and are rebuilt from the raw export, so they are not tracked.
analysis/*.csv
!analysis/label_corrections.csv
